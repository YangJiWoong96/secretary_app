name: timely_agent
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  device_info_plus: ^10.0.0
  cupertino_icons: ^1.0.8
  geolocator: ^10.1.0
  device_calendar: ^4.0.0
  http: ^1.2.1
  permission_handler: ^12.0.0
  flutter_foreground_task: ^9.1.0
  flutter_dotenv: ^5.1.0 # aws 통신서버를 위한 .env 임폴트용
  shared_preferences: ^2.0.15
  sleek_circular_slider: ^2.0.1
  timezone: any

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
    - .env
